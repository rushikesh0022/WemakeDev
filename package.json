{
  "name": "zaply",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter @zaply/web dev",
    "build": "pnpm --filter @zaply/web build",
    "lint": "pnpm --filter @zaply/web lint",
    "test": "pnpm --filter @zaply/web test",
    "aws:validate": "aws cloudformation validate-template --template-body file://infra/aws/pico-foundation.yaml --region us-east-1"
  }
}
